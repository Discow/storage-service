package com.example.server.util;

/**
 * 用于定义常量
 */

public interface Constants {
    interface Settings {
        String STORAGE_PATH_KEY = "storage.path";
        String OS_KEY = "os";
    }

    interface User {
        Long PUBLIC_USER_ID = 1L;
    }
}
